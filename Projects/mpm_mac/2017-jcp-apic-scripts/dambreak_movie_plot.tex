\documentclass[tikz,crop,border=0pt]{standalone}
\usepackage{pgfplots}

\newcommand{\dbplot}[1]{
\begin{tikzpicture}
\begin{axis}[
    axis background/.style={fill=gray},
    enlargelimits=false,
    xmin=0,
    xmax=1,
    ymin=0,
    ymax=1,
    colormap/jet,
    tick scale binop=\times,
    scale ticks above exponent=2,
    y tick label style={
        /pgf/number format/.cd,
            fixed,
            fixed zerofill,
            precision=2,
        /tikz/.cd
    }
]
    \addplot[
        scatter,scatter src=explicit,
        scatter/use mapped color={draw opacity=0,fill=mapped color}, % remove boundary
        point meta min=-2,point meta max=2,
        only marks,mark options={mark size=0.5pt}] table[x=x,y=y,meta=vort]{#1.txt};
\end{axis}
\end{tikzpicture}
}

\begin{document}
\pgfplotsset{width=10cm,height=8cm,compat=1.13}
XXX
\end{document}
