USE_COMPRESSION=yes

# CUT OFF POINT (lines above will be preserved, lines below will be overwritten)
# Set any necessary USE_* variables (e.g. USE_OPENGL=yes) above CUT OFF POINT.
# GENERATED BY create_makefile.sh particle_octree main.cpp

#TARGETS = process_levelsets
#MAIN_SRC = process_levelsets.cpp
TARGETS = octree_removed_particles mmc prune_particles
MAIN_SRC = octree_removed_particles.cpp mmc.cpp prune_particles.cpp

LINK_FLAGS += -static

PUBLIC_LIBRARY_DIR = ../../Public_Library
PERSONAL_LIBRARIES_DIR = ../../Personal_Libraries

LIBRARIES = PhysBAM

include ../../Public_Library/Makefile.common
